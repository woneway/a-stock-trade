# AI化游资交易系统架构设计 v2.0

> 设计目标：让AI成为执行型游资，用户成为全局型游资/教练

---

## 一、核心定位

| 角色 | 定位 | 职责 |
|------|------|------|
| **AI Agent** | 执行型游资 | 市场分析、计划生成、执行下单、学习优化 |
| **用户** | 全局型教练 | 设定策略风格、审核计划、关键时刻干预、全局监控 |

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           用户层 (User Layer)                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  计划审核   │  │  干预执行   │  │  策略配置   │  │  监控看板   │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↑
                              [审核/干预交互]
                                    ↑
┌─────────────────────────────────────────────────────────────────────────┐
│                         AI Agent 层 (AI Layer)                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      🤖 AI交易大脑                               │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │   │
│  │  │  市场感知    │  │  决策引擎    │  │  执行控制    │          │   │
│  │  │  (Perception)│  │  (Brain)     │  │  (Action)    │          │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ 市场分析    │  │ 计划生成    │  │ 交易执行    │  │ 学习优化    │   │
│  │ Agent       │  │ Agent       │  │ Agent       │  │ Agent       │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↑
                              [数据/指令流动]
                                    ↑
┌─────────────────────────────────────────────────────────────────────────┐
│                        服务层 (Service Layer)                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ 数据采集    │  │ 计划管理    │  │ 持仓管理    │  │ 交易记录    │   │
│  │ Service     │  │ Service     │  │ Service     │  │ Service     │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↑
┌─────────────────────────────────────────────────────────────────────────┐
│                        数据层 (Data Layer)                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  股票数据  │  │  交易数据  │  │  策略配置  │  │  学习数据  │   │
│  │  (Market)  │  │  (Trade)   │  │  (Config)  │  │  (Memory)  │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 三、核心模块设计

### 3.1 AI Agent 架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     🤖 AI 交易大脑 (TradingBrain)               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │   输入      │───▶│   思考      │───▶│   输出      │         │
│  │  (Input)   │    │  (Think)    │    │  (Output)   │         │
│  └─────────────┘    └─────────────┘    └─────────────┘         │
│         │                  │                  │                 │
│         ▼                  ▼                  ▼                 │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │ 市场数据    │    │ 决策树/     │    │ 交易计划    │         │
│  │ 历史战绩    │    │ LLM推理     │    │ 执行指令    │         │
│  │ 用户偏好    │    │ 规则引擎    │    │ 分析报告    │         │
│  └─────────────┘    └─────────────┘    └─────────────┘         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 四大AI Agent设计

#### (1) 市场分析 Agent (Market Analyst)

**职责**：理解当前市场状态

**输入**：
- 实时行情数据（价格、量能、涨跌幅）
- 涨停/炸板数据
- 板块资金流向
- 龙虎榜数据

**输出**：
```python
{
    "market_sentiment": " bullish | bearish | neutral ",  # 市场情绪
    "hot_sectors": ["AI", "新能源"],                      # 热门板块
    "risk_level": 0.7,                                   # 风险等级 0-1
    "opportunity_score": 0.6,                            # 机会评分 0-1
    "market_cycle": "recovery | peak | decline | bottom", # 情绪周期
    "key_signals": [                                     # 关键信号
        {"type": "limit_up", "count": 45},
        {"type": "limit_down", "count": 8},
    ]
}
```

**决策逻辑**：
- 涨停数量 > 30 → 高潮 → 谨慎
- 炸板率 > 40% → 冰点 → 休息
- 板块持续性 > 2天 → 趋势确认

---

#### (2) 计划生成 Agent (Plan Generator)

**职责**：根据市场分析和用户风格生成交易计划

**输入**：
- 市场分析结果
- 用户策略配置（风险偏好、模式偏好）
- 目标股池
- 账户资金状况

**输出**：
```python
{
    "plans": [
        {
            "stock_code": "600519",
            "stock_name": "贵州茅台",
            "trade_mode": "低吸",           # 低吸/半路/打板
            "target_price": 1680.0,
            "position_ratio": 0.15,         # 15%仓位
            "stop_loss_price": 1562.0,      # -7%
            "take_profit_1": 1764.0,       # +5%
            "take_profit_2": 1848.0,       # +10%
            "validation_conditions": {      # 验证条件
                "open_pct": "<3%",
                "volume": ">1.5亿"
            },
            "valid_time_range": "09:30-10:30",
            "logic": "龙头回调企稳，量能配合",
            "confidence": 0.75,              # AI信心指数
            "approval_status": "pending"     # pending/approved/rejected
        }
    ],
    "risk_warning": "当前市场处于高潮期，建议仓位控制在30%以内",
    "plan_count": 3
}
```

**生成策略**：
- 激进模式：高频、追龙头、重仓位
- 稳健模式：低吸、回调买、分散持仓
- 保守模式：只做首板、严格止损

---

#### (3) 交易执行 Agent (Execution Agent)

**职责**：监控计划执行，处理买卖时机

**执行模式**：
```
┌─────────────────────────────────────────────────────────────┐
│                    执行决策流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   计划触发 ──▶ 验证条件检查 ──▶ 时段检查                   │
│       │              │              │                       │
│       │              │              ▼                       │
│       │              │         是否在有效时段?               │
│       │              │              │                       │
│       │              ▼              ▼                       │
│       │         条件满足?        是                         │
│       │              │              │                       │
│       ▼              ▼              ▼                       │
│   ┌─────────────────────────────────────────┐             │
│   │         权限检查                         │             │
│   │  AI自动执行? 用户确认? 禁止执行?        │             │
│   └─────────────────────────────────────────┘             │
│                    │                                       │
│         ┌─────────┼─────────┐                            │
│         ▼         ▼         ▼                            │
│      自动执行   提醒用户   禁止执行                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**提醒机制**：
| 触发条件 | 提醒级别 | 动作 |
|----------|----------|------|
| 价格触及买入价 | 中 | 提醒用户确认 |
| 验证条件满足 | 强 | 强化提醒 |
| 止损预警(-5%) | 强 | 强提醒 |
| 触及止损价 | 紧急 | 自动记录+提醒 |

---

#### (4) 学习优化 Agent (Learning Agent)

**职责**：从历史交易中学习，不断优化策略

**学习维度**：
```python
{
    "performance_metrics": {
        "win_rate": 0.55,           # 胜率
        "profit_loss_ratio": 1.8,   # 盈亏比
        "plan_completion_rate": 0.7 # 计划完成率
    },
    "pattern_learned": [
        {
            "pattern": "龙头股回调低吸",
            "success_rate": 0.68,
            "best_timing": "10:00-10:30"
        },
        {
            "pattern": "首板打板",
            "success_rate": 0.45,
            "best_sector": "AI"
        }
    ],
    "recommendations": [
        "建议增加低吸模式权重，当前胜率更高",
        "避免在下午14:30后打板，历史成功率低"
    ]
}
```

---

## 四、人机协作设计

### 4.1 协作流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                        人机协作闭环                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│     ┌──────────────┐                                                │
│     │   用户配置   │                                                │
│     │  策略风格    │                                                │
│     └──────┬───────┘                                                │
│            │                                                        │
│            ▼                                                        │
│     ┌──────────────┐                                                │
│     │   AI生成    │                                                │
│     │   交易计划  │                                                │
│     └──────┬───────┘                                                │
│            │                                                        │
│            ▼                                                        │
│     ┌──────────────────────────────────┐                           │
│     │        权限判断                  │                           │
│     │  自动执行? → 直接执行            │                           │
│     │  需确认?   → 推送用户           │                           │
│     │  禁止     → 记录并跳过          │                           │
│     └──────────────────────────────────┘                           │
│            │                                                        │
│            ▼                                                        │
│     ┌──────────────┐     ┌──────────────┐                         │
│     │  用户审核    │────▶│  用户干预    │                         │
│     │  批准/修改  │     │  手动执行    │                         │
│     └──────────────┘     └──────────────┘                         │
│            │                                                        │
│            ▼                                                        │
│     ┌──────────────┐                                                │
│     │   执行/记录  │                                                │
│     └──────────────┘                                                │
│            │                                                        │
│            ▼                                                        │
│     ┌──────────────┐                                                │
│     │   AI学习    │                                                │
│     └──────────────┘                                                │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2 权限控制

| 操作类型 | 权限等级 | 说明 |
|----------|----------|------|
| 买入 | auto | AI直接执行 |
| 买入 | confirm | 需用户确认 |
| 买入 | manual | 仅手动 |
| 卖出-止盈 | auto | AI直接执行 |
| 卖出-止损 | auto | AI直接执行 |
| 卖出-手动 | confirm | 需用户确认 |

### 4.3 审核功能

**AI计划审核面板**：
```
┌─────────────────────────────────────────────────────────────┐
│                    今日AI交易计划 (3)                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  [600519] 贵州茅台                                          │
│  模式: 低吸 | 目标价: 1680 | 仓位: 15%                      │
│  信心指数: ⭐⭐⭐⭐☆ (0.75)                                 │
│  逻辑: 龙头回调企稳，量能配合                                 │
│  ───────────────────────────────────────────────────────    │
│  [✓ 批准] [✗ 拒绝] [📝 修改] [⏰ 延后]                       │
│                                                              │
│  ───────────────────────────────────────────────────────    │
│                                                              │
│  [300750] 宁德时代                                          │
│  模式: 半路 | 目标价: 185 | 仓位: 10%                        │
│  信心指数: ⭐⭐⭐☆☆ (0.55)                                  │
│  逻辑: 新能源板块持续性确认                                   │
│  ───────────────────────────────────────────────────────    │
│  [✓ 批准] [✗ 拒绝] [📝 修改] [⏰ 延后]                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、数据模型扩展

### 新增数据表

#### (1) AIStrategyConfig - AI策略配置
```python
class AIStrategyConfig(Base):
    __tablename__ = "ai_strategy_config"

    id = Column(Integer, primary_key=True)

    # 风格配置
    risk_preference = Column(String(20))      # aggressive/moderate/conservative
    trade_mode_preference = Column(String(50)) # low_buy/breakthrough/limit_up
    max_position_per_trade = Column(Float)   # 单笔最大仓位
    max_total_position = Column(Float)        # 总仓位上限

    # 执行权限
    auto_buy_enabled = Column(Boolean, default=False)
    auto_sell_enabled = Column(Boolean, default=False)
    confirm_before_trade = Column(Boolean, default=True)

    # 学习配置
    learning_enabled = Column(Boolean, default=True)
    min_samples_to_learn = Column(Integer, default=20)

    # 时间配置
    trading_hours_start = Column(String(10))  # "09:30"
    trading_hours_end = Column(String(10))   # "14:57"
    updated_at = Column(DateTime)
```

#### (2) AIMarketAnalysis - AI市场分析
```python
class AIMarketAnalysis(Base):
    __tablename__ = "ai_market_analysis"

    id = Column(Integer, primary_key=True)
    analysis_date = Column(Date)

    # 分析结果
    market_sentiment = Column(String(20))      # bullish/bearish/neutral
    hot_sectors = Column(JSON)                # ["AI", "新能源"]
    risk_level = Column(Float)                # 0-1
    opportunity_score = Column(Float)         # 0-1
    market_cycle = Column(String(20))         # recovery/peak/decline/bottom
    key_signals = Column(JSON)

    # AI判断
    recommendation = Column(String(50))        # heavy/medium/light/empty
    reasoning = Column(Text)

    created_at = Column(DateTime)
```

#### (3) AITradeApproval - AI交易审批
```python
class AITradeApproval(Base):
    __tablename__ = "ai_trade_approval"

    id = Column(Integer, primary_key=True)
    plan_id = Column(Integer, ForeignKey("trading_plans.id"))

    # AI生成信息
    ai_confidence = Column(Float)              # AI信心指数
    ai_reasoning = Column(Text)               # AI决策理由
    risk_warning = Column(String(200))       # 风险提示

    # 审批状态
    approval_status = Column(String(20))       # pending/approved/rejected/modified
    user_comment = Column(Text)
    approved_at = Column(DateTime)

    # 执行信息
    execution_status = Column(String(20))      # waiting/executed/cancelled
    executed_at = Column(DateTime)
```

#### (4) AIExecutionLog - AI执行日志
```python
class AIExecutionLog(Base):
    __tablename__ = "ai_execution_log"

    id = Column(Integer, primary_key=True)

    # 触发信息
    trigger_type = Column(String(50))         # price_reach/condition_met/time_trigger
    trigger_data = Column(JSON)

    # 决策信息
    decision = Column(String(20))              # execute/wait/skip
    decision_reason = Column(Text)

    # 执行结果
    result = Column(String(20))               # success/failed/cancelled
    error_message = Column(Text)

    created_at = Column(DateTime)
```

---

## 六、API设计

### 6.1 AI相关API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/ai/analyze-market | AI市场分析 |
| POST | /api/v1/ai/generate-plans | AI生成交易计划 |
| POST | /api/v1/ai/approve-plan | 用户审批AI计划 |
| GET | /api/v1/ai/pending-approvals | 待审批计划列表 |
| POST | /api/v1/ai/execute | 执行交易（AI或手动） |
| GET | /api/v1/ai/performance | AI绩效报告 |
| GET | /api/v1/ai/learning-insights | AI学习洞察 |

### 6.2 核心API示例

#### AI市场分析
```bash
POST /api/v1/ai/analyze-market
Response:
{
    "market_sentiment": "bullish",
    "hot_sectors": ["AI", "半导体"],
    "risk_level": 0.6,
    "opportunity_score": 0.7,
    "market_cycle": "recovery",
    "recommendation": "medium",
    "reasoning": "涨停45家，炸板率25%，市场情绪良好"
}
```

#### AI生成计划
```bash
POST /api/v1/ai/generate-plans
Body: {
    "stock_pool": ["600519", "300750"],  # 可选：指定股池
    "max_plans": 5
}
Response:
{
    "plans": [
        {
            "id": 101,
            "stock_code": "600519",
            "trade_mode": "低吸",
            "target_price": 1680,
            "position_ratio": 0.15,
            "ai_confidence": 0.75,
            "approval_status": "pending"
        }
    ],
    "risk_warning": "建议仓位控制在30%以内"
}
```

---

## 七、技术架构

### 7.1 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端框架 | FastAPI | 现有保持 |
| AI推理 | LLM (OpenAI/Claude/本地) | 交易决策推理 |
| 向量数据库 | ChromaDB | 存储历史模式学习 |
| 任务队列 | Celery + Redis | 定时任务和异步执行 |
| 实时推送 | WebSocket | 实时提醒和状态同步 |
| 缓存 | Redis | 行情缓存和状态缓存 |

### 7.2 部署架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户端                                │
│   Web/App                                                    │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      API Gateway (Nginx)                   │
└────────────────────────────┬────────────────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         ▼                   ▼                   ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Web API   │     │  WebSocket  │     │  Task Queue │
│  (FastAPI) │     │  Server     │     │  (Celery)   │
└──────┬──────┘     └─────────────┘     └──────┬──────┘
       │                                       │
       ▼                                       ▼
┌─────────────┐                      ┌─────────────┐
│  SQLite/    │                      │  Redis      │
│  PostgreSQL │                      │  (Celery)   │
└─────────────┘                      └─────────────┘
                                           │
                                           ▼
                                    ┌─────────────┐
                                    │  AI Service │
                                    │  (LLM API)  │
                                    └─────────────┘
```

---

## 八、核心工作流

### 每日AI交易流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                        每日AI交易流程                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────┐                                                │
│  │  09:15 开盘前   │                                                │
│  │  AI市场分析     │ ──▶ 输出：市场情绪、风险等级、推荐仓位         │
│  └────────┬────────┘                                                │
│           │                                                          │
│           ▼                                                          │
│  ┌─────────────────┐                                                │
│  │  AI生成交易计划 │ ──▶ 基于市场分析+用户风格+目标股池            │
│  └────────┬────────┘                                                │
│           │                                                          │
│           ▼                                                          │
│  ┌─────────────────────────────────────────┐                        │
│  │  权限检查                               │                        │
│  │  auto → 执行                           │                        │
│  │  confirm → 推送给用户审核              │                        │
│  └────────┬────────────────────────────────┘                        │
│           │                                                          │
│           ▼                                                          │
│  ┌─────────────────┐                                                │
│  │  09:30-15:00    │                                                │
│  │  实时监控执行   │                                                │
│  │  - 价格监控     │                                                │
│  │  - 条件验证     │                                                │
│  │  - 提醒推送     │                                                │
│  └────────┬────────┘                                                │
│           │                                                          │
│           ▼                                                          │
│  ┌─────────────────┐                                                │
│  │  15:00 盘后     │                                                │
│  │  AI学习优化     │                                                │
│  │  - 统计绩效     │                                                │
│  │  - 模式学习     │                                                │
│  │  - 建议优化     │                                                │
│  └─────────────────┘                                                │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 九、实施路线

### Phase 1: 基础AI化 (MVP)
- [x] 现有数据模型扩展
- [x] AI策略配置管理
- [x] AI市场分析接口
- [x] AI计划生成（简单规则+LLM）
- [x] 计划审批流程

### Phase 2: 执行自动化
- [x] 实时价格监控
- [x] 条件触发引擎
- [x] WebSocket实时推送
- [x] 权限控制系统

### Phase 3: 智能化升级
- [x] 历史模式学习
- [x] 绩效分析
- [x] 策略自动优化
- [x] 多Agent协作

---

## 十、总结

这个架构的核心思想：

1. **AI是执行者**：AI负责看盘、分析、生成计划、到价执行
2. **用户是教练**：用户设定风格、审核计划、关键时刻干预
3. **渐进式自动化**：从"AI建议+用户执行"到"AI自动执行+用户监督"
4. **持续学习**：AI从每次交易中学习，不断优化策略

这样设计的好处：
- 保留用户的主导权
- 降低用户的操作负担
- AI可以24小时不间断工作
- 通过学习不断提升效果
